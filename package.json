{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",  
    "deploy": "nuxt build && nuxt export && gh-pages -d dist"
  },
  "devDependencies": {
    "@invictus.codes/nuxt-vuetify": "^0.2.20",
    "@nuxtjs/i18n": "^8.0.0-beta.12",
    "@types/node": "^18",
    "dotenv": "^16.0.3",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.5.0",
    "nuxt-mail": "^4.0.1"
  
  },
  "overrides": {
    "vue": "latest"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.11",
    "pinia": "^2.1.3"
  },
  "homepage": "https://klavas35.github.io/resumesite"
}
